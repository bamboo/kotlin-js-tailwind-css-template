/** @type {import('tailwindcss').Config} */
module.exports = {
  // content files should be setup in `build.gradle.kts`
  content: [],
  // remaining customizations should work as expected
  theme: {
    colors: {
      'blue': '#209BC4'
    },
    extend: {},
  },
  plugins: [],
}

